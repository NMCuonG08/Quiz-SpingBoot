package com.example.WebSocket;

/**
 * Disabled listener. Obsolete since migration to Netty Socket.IO.
 */
public class WebSocketEventListener {
    // Left intentionally blank to avoid IDE sync bugs when deleting files
}
